import pytest
from sqlalchemy.ext.asyncio import AsyncSession
from unittest.mock import patch
from src.core.menu_engine import MenuEngine, MenuNode

@pytest.mark.asyncio
async def test_load_menu_tree():
    # این تست نیاز به دیتابیس واقعی یا mock دارد. فعلاً ساختار را نشان می‌دهیم.
    pass
